{
  "name": "lbs-static-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "start": "npx serve@14 . --listen $PORT --single"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
