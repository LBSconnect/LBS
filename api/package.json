{
  "name": "lbs-contact-api",
  "version": "1.0.0",
  "description": "LBS contact form API — sends email via Resend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "resend": "^3.2.0",
    "svix": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
